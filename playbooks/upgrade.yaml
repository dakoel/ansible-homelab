---
- name: Upgrade packages
  hosts: all
  gather_facts: false
  roles:
    - detect_ssh_port
  tasks:
    - name: Update and upgrade apt packages
      ansible.builtin.apt:
        upgrade: true
        update_cache: true
      become: true
