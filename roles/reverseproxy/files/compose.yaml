---
services:
  nginx:
    image: nginx:latest
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./logs:/var/log/nginx
      - ./certs:/etc/nginx/certs
    ports:
      - "80:80"
      - "443:443"
    restart: always
